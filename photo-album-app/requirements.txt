# --- 必須(これだけで心臓部ループはフォールバック動作する) ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
Pillow>=10.0
numpy>=1.26
opencv-python-headless>=4.9

# --- 任意(CLIPによる意味的キーワード検索・ゼロショット被写体タグを有効化) ---
# 未インストールでもアプリは起動し、メタ照合フォールバックで動作する。
# 有効化するには次の2行のコメントを外して pip install:
#   初回はモデル(数百MB)を自動ダウンロードし、初回ロードは重い。
# torch>=2.1
# open_clip_torch>=2.24
